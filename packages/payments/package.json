{
  "name": "@run402/payments",
  "version": "0.0.0",
  "private": true,
  "description": "Provider-agnostic payments abstraction (mock, Stripe, future x402).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "clean": "rm -rf .turbo dist"
  },
  "dependencies": {
    "@run402/utils": "workspace:*",
    "stripe": "^17.5.0"
  },
  "devDependencies": {
    "@run402/config": "workspace:*",
    "@types/node": "^22.20.0",
    "eslint": "^9.18.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
