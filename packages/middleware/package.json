{
  "name": "run402",
  "version": "0.0.0",
  "private": true,
  "description": "Run402 API monetization middleware. Wrap a route, set a price, get paid.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./express": "./src/express.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "clean": "rm -rf .turbo dist"
  },
  "devDependencies": {
    "@run402/config": "workspace:*",
    "@types/node": "^22.20.0",
    "eslint": "^9.18.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
